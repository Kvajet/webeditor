import { CTextEditor } from './CTextEditor'

// try {
//     const editor = new CTextEditor();
//     editor.Process();
// } catch( e ) {
//     console.log( `[ERROR] ${ ( <Error>e ).message }` );
// }

const editor = new CTextEditor();
editor.Process();
